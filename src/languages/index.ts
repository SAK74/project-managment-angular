// import polish from './polish';

// export default function translator(text: string, lang: LangType) {
//   switch (lang) {
//     case 'pl':
//       return polish[text];
//     default:
//       return text;
//   }
// }

export type LangType = 'en' | 'pl';
